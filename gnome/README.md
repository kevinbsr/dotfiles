# How to restore gnome settings
```bash
dconf load / < gnome/dconf-settings.ini
```

# How to restore gnome extensions
```bash
dconf load /org/gnome/shell/extensions/ < gnome/extensions-settings.ini
```
